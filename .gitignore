# Sensitive/private
# MEK's long-term memory (top-level only)
/MEMORY.md
/HEARBEAT.md
# MEK's memory (top-level only)
/memory/
# Agent memory dirs are tracked (agents/<name>/memory/)
/state/

# Large/generated artifacts (adjust as needed)
/assets/

# Git repos with their own git tracking
/repos/

# Secrets/config
openclaw.json
openclaw.json.bak*
config/openclaw.json
config/openclaw.template.json
*.env
*.pem
*.key

# Dont know what it is but being safe
.pi/

# Runtime/cache/temp
.openclaw/
**/__pycache__/
*.pyc
node_modules/
.tmp/
TEMP_ARTIFACTS.md

# OS/editor noise
.DS_Store
*.swp
*.swo
